# 需求文档

## 架构和实现方案

1、阅读/需求。增加相同布局导航页和内页（内页采用mdx）,也需要实现多语言，创建方案文档（对比方案优劣）

2、
src/
  ├── app/
  │   ├── api/
  │   │   └── guides/
  │   │       ├── route.ts
  │   │       └── [slug]/route.ts
  │   └── guides/
  │       ├── layout.tsx
  │       ├── page.tsx
  │       └── [slug]/page.tsx
  ├── components/
  │   └── mdx/
  │       └── index.tsx
  └── lib/
      └── mdx.ts

  content/
  ├── en/
  │   └── guide/
  │       └── beginner.mdx
  └── zh/
      └── guide/
          └── beginner.mdx

1、解释这些文件作用？
2、为什么需要api? 我的页面都是静态即可


  📁 核心文件
  ┌──────────────────────────┬─────────────────────────────────────┬──────────┐
  │           文件           │                作用                 │ 是否必需 │
  ├──────────────────────────┼─────────────────────────────────────┼──────────┤
  │ lib/mdx.ts               │ MDX 工具函数（读取、解析 MDX 文件） │ ✅ 必需  │
  ├──────────────────────────┼─────────────────────────────────────┼──────────┤
  │ components/mdx/index.tsx │ 自定义 MDX 组件（Alert、样式等）    │ ✅ 必需  │
  ├──────────────────────────┼─────────────────────────────────────┼──────────┤
  │ content/en/guide/*.mdx   │ 英文攻略内容                        │ ✅ 必需  │
  ├──────────────────────────┼─────────────────────────────────────┼──────────┤
  │ content/zh/guide/*.mdx   │ 中文攻略内容                        │ ✅ 必需  │
  └──────────────────────────┴─────────────────────────────────────┴──────────┘
  📄 页面文件
  ┌────────────────────────────┬──────────────┬───────────────┬──────────────────┐
  │            文件            │     作用     │   当前实现    │      应该是      │
  ├────────────────────────────┼──────────────┼───────────────┼──────────────────┤
  │ app/guides/layout.tsx      │ 攻略页面布局 │ ✅ 正确       │ 服务端组件       │
  ├────────────────────────────┼──────────────┼───────────────┼──────────────────┤
  │ app/guides/page.tsx        │ 攻略列表页   │ ❌ 客户端组件 │ 应改为服务端组件 │
  ├────────────────────────────┼──────────────┼───────────────┼──────────────────┤
  │ app/guides/[slug]/page.tsx │ 攻略详情页   │ ❌ 客户端组件 │ 应改为服务端组件 │
  └────────────────────────────┴──────────────┴───────────────┴──────────────────┘
  🚫 不必要的文件
  ┌────────────────────────────────┬───────────────────┬──────────────────────────────────────┐
  │              文件              │       作用        │             为什么不需要             │
  ├────────────────────────────────┼───────────────────┼──────────────────────────────────────┤
  │ app/api/guides/route.ts        │ API：获取攻略列表 │ ❌ 页面可以直接调用 getAllGuides()   │
  ├────────────────────────────────┼───────────────────┼──────────────────────────────────────┤
  │ app/api/guides/[slug]/route.ts │ API：获取单个攻略 │ ❌ 页面可以直接调用 getGuideBySlug() │
  └────────────────────────────────┴───────────────────┴──────────────────────────────────────┘

5、我不仅需要guide，还有其他wiki、code等导航页，导航和文章页能不能复用一套
src/app/[locale]/guides/[slug]/page.tsx，我希望所有目录下的文章都遵循同一个模板（不需要每级目录下都单独定义），参考docs/0113Heartopia/src/app/[locale]/[...slug]/page.tsx的
实现方式（参考路径实现，但不要参考文章页模板格式和内容）

我经常需要修改多语言，思考怎么实现最小修改：
1 outing.ts 的 locales 数组
2 src/components/Navigation.tsx的数组

## 页面优化需求

文章页：
1、不需要start here这个组件展示，占据无效空间。
2、标题和描述，离下方正文的距离太远，要缩短一半。
3、最下方的文章推荐，改成动态推荐同主题下的其他文章，不要在mdx里写死。
<CTAButtons
  buttons={[
    { label: "More Guides", href: "/en/guides" },
    { label: "View Unit Tier List", href: "/en/units" },
    { label: "Get Codes", href: "/en/codes", variant: "primary" }
  ]}
/>
4、我默认语言是英文，当英语时，不显示语言前缀

1 用户希望详情页 Hero 区域的背景渐变颜色和背景文字能够
**从文章的背景图片动态提取**，而不是硬编码。

1 网站的图标 在哪里配置？还有导航栏左侧的图标，在哪配置的？
2 继续增加2篇guide文章。

导航栏左侧的图标在src/components/Navigation.tsx:64-66：
<div className="w-10 h-10 bg-[#43b176] rounded-lg flex items-center justify-center font-bold text-xl"> U <div>

网站 favicon：Next.js 15 使用文件约定，需要在 src/app 目录下放置：
  - favicon.ico - 网站图标
  - icon.png 或 icon.svg - 应用图标
  - apple-icon.png - Apple 设备图标

All Guides的文章展示，改为一行三个

文章页：
表格，表头没有样式，且表格里的文字要居中。
Faq里的字体加粗 **** 没有渲染出来。
链接文字，在黑色背景下看不清楚。
导航页：
如果文章数<2, 即All Guides下如果一篇文章也没有，可以不展示All Guides这个标题。

假设某个导航页只有一篇文章，则直接显示这个文章页。

2、亮色和黑色、颜色设置
我经常需要修改颜色配置，在哪里进行颜色设置呢？是否已经符合最小配置原则

去掉页面所有颜色的硬编码，统一采用配置文件方式。

## 内容生成和管理

1、生成文章
prompt：特别长的标题，游戏名+关键词即可-补充 形式
2、修改首页、导航页文案

给我将yotube视频，寻找合适位置插进这个文章页：
https://www.youtube.com/watch?v=qmksbJsQkmA

修改tools/articles/modules/generation/prompt-template.txt，
让它支持生成content/en/guide/progression-guide.mdx格式文章，例如组件的语法和使用，保证不用错，平衡质量和提示词字数。

yotube每关键词搜索到前5个视频，过滤时长大于1h，然后按照排名选择前2个，如果都大于1h，只选择一个

3、tools/articles/modules/generation/prompt-template.txt
修改文章标题：格式固定为：主关键词：描述xxxx（60字左右）
4、文章标题是主关键词：描述xxxx。
src/app: 修改导航、博客页展示时，只保留：之前主关键词即可

2、修改tools/articles/modules/generation的代码生成逻辑，之前依赖
tools/articles/modules/generation/内页.json和tools/articles/modules/generation/youtube_data.csv
现在修改成：依赖tools/keywords.json和tools/content_pipeline/out/merged/关键词.json文件，写一个完成清晰重构方案。
最后输出的路径也到了项目根目录content/

tools/articles/modules/generation/prompt-template.txt提示词：
同一个视频，只能使用一次，禁止使用多次。
Callout组件，每个section里都必须使用一次。
下面这些要求，可以移到提示词最上面，避免没有遵守：
**组件使用强制要求（重要）**：
每篇文章必须至少使用以下所有组件各一次：
- ✅ **Section** - 每个主要章节（必用，通常 4-6 个）
- ✅ **Callout** - 至少 2-3 个（使用不同 type: tip/warning/info/success）
- ✅ **CardGrid/Card** - 至少 1 组（3-4 个卡片，展示并列对比）
- ✅ **Steps** - 至少 1 个步骤流程（3-5 个步骤）
- ✅ **Checklist** - 至少 1 个清单（3-5 个项目）
- ✅ **FAQ** - 至少 1 个（3-4 组问答）
- ✅ **表格** - 至少 3-5 个（必须包含评级或对比数据）

## 工具和流程优化

tools/content_pipeline/：
2个分类Server&Account、Community关键词，不需要进行视频和网页搜索，以及将使用的各种参数，如每次跑多少网页，视频等，统一放到一个环境变量文件；

1、搜索去重
修改tools/content_pipeline/web_search.py：
对所有关键词通过web搜索的网页，先进行去重，再进行批量化获取markdown内容，避免重复，浪费流量。

硬编码到本地。

本地代理池测试

header, nav, footer, aside, .sidebar, #sidebar, .advertisement, .ad, [class*="ad-"], [id*="ad-"],
  .cookie-banner, .cookie-consent, .social-share, #comments, .comments, .comment-form,
  [class*="newsletter"], [class*="subscribe"]

out/temp/{url_hash}.json，可以改成网页标题名嘛

未来工作：
定时监听机制：自动更新、增加内页内容。

哪里获取similar web api，查询最新关键词。

tools/content_pipeline/extract.py:
抽取过程,要先对所有关键词的所有视频、网页这两个分别去重,然后再批量抽取。

根据视频里的内容，进行页面重构。

阅读修改tools/content_pipeline/out/pending_review.json：
判断关键词收集的资料，是否相关，只保留相关内容。
如果一个关键词下，所有资料都是不相关的，则直接去掉这个关键词。

重试次数和时间，记录在配置文档。

cd /Users/libin91/Documents/GameProjects/0120Apogea/tools/articles/modules/generation && python3 generate-articles.py --test
如果都执行成功了，且格式正确。

再去调用函数生成所有文章。如果有一个失败，都要禁止执行后续所有文章
python3 tools/articles/modules/generation/generate-articles.py

collect.py阶段，可以不使用代理吗？针对这一个阶段是否开启进行配置是否开启

## SEO和数据分析

参考：需求/00基础信息.md
2个数据分析脚本（Google Analytics 和 Microsoft Clarity）
添加2个数据分析脚本，并修改.env.local
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-H9PJYK9BFD"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-H9PJYK9BFD');
</script>
<script type="text/javascript">
    (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
    })(window, document, "clarity", "script", "v71fu47ax0");
</script>


增加广告，并修改.env.local
为了让您的网站可以开始展示广告，请将下面的代码复制并粘贴到您网站中每个网页上的 <head> 和 </head> 标记之间。 详细了解 AdSense 代码。
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7733402184034568"
     crossorigin="anonymous"></script>

为了让您的网站可以开始展示广告，请将以下文本复制并粘贴到每个 ads.txt 文件中，然后上传到您网站的根目录。如果您已有一个 ads.txt 文件，只需将该文本粘贴到每个文件中即可。 详细了解如何创建 ads.txt 文件。
google.com, pub-7733402184034568, DIRECT, f08c47fec0942fa0

为了让您的网站可以开始展示广告，请将下面的 HTML <meta> 标记复制并粘贴到您网站中每个网页上的 <head></head> 标记之间。 详细了解如何使用 HTML <meta> 标记关联网站。
<meta name="google-adsense-account" content="ca-pub-7733402184034568">


需求/0202animeparadox 研究竞对的源代码,分析与当前项在在seo上的区别, 为什么它的收录远比我快很多,尽管我上线早(首页\导航页\文章页单独对比。


1、加内链，测试导航页收录情况
2、解决内页收录问题

3、模板自动化
-- 内页自动化、skills
4、服务器
部署、趋势工具

5、验证新词
做大站：自动化建站
加内页，测试趋势


需求/0202animeparadox，再次研究竞对的源代码,分析与当前项在seo上的剩余区别

## 已完成任务

0 - 立即修复（2个任务）
  ✅ 任务 #1: 完善根布局元数据配置
  - 文件：src/app/[locale]/layout.tsx
  - 添加了 keywords 数组（10个关键词）
  - 添加了 robots 配置（index, follow, googleBot）
  - 完善了 openGraph 配置（type, locale, url, siteName, images）
  - 添加了 twitter 卡片配置（card, title, description, images, creator）

  ✅ 任务 #2: 添加首页结构化数据 JSON-LD
  - 文件：src/app/[locale]/page.tsx
  - 添加了 WebSite 类型结构化数据（包含 SearchAction）
  - 添加了 Organization 类型结构化数据（包含 logo 和 sameAs）
  - 使用 @graph 格式组织多个实体

  P1 - 高优先级（5个任务）

  ✅ 任务 #3: 创建文章结构化数据组件
  - 文件：src/components/content/ArticleStructuredData.tsx（新建）
  - 生成 Article 类型的 JSON-LD
  - 包含 headline, description, image, datePublished, dateModified, author, publisher,
  mainEntityOfPage
  - 支持多语言 URL

  ✅ 任务 #4: 在文章详情页集成结构化数据
  - 文件：src/app/[locale]/[...slug]/page.tsx
  - 在 renderDetailPage 函数中集成 ArticleStructuredData 组件
  - 同时处理主语言和回退语言的情况

  ✅ 任务 #5: 创建列表页结构化数据组件
  - 文件：src/components/content/ListStructuredData.tsx（新建）
  - 生成 ItemList 类型的 JSON-LD
  - 包含 itemListElement 数组（ListItem with position, url, name）
  - 支持多语言 URL

  ✅ 任务 #6: 在列表页集成结构化数据
  - 文件：src/app/[locale]/[...slug]/page.tsx
  - 在 renderListPage 函数中集成 ListStructuredData 组件
  - 同时处理正常和回退情况

  ✅ 任务 #7: 优化 sitemap 优先级和更新频率
  - 文件：src/app/sitemap.ts
  - 静态页面根据重要性区分优先级：
    - about: 0.6 (monthly)
    - privacy/terms/copyright: 0.3 (yearly)
  - 内容类型优先级配置：
    - codes/tier-list: 0.9 (daily/weekly)
    - units/guide: 0.8 (weekly)
    - farming/gameplay: 0.7 (weekly/monthly)
    - resources: 0.6 (monthly)
    - community: 0.5 (monthly)

  P2 - 中优先级（2个任务）

  ✅ 任务 #8: 添加性能优化配置到 Next.js
  - 文件：next.config.mjs
  - 添加 compress: true（启用压缩）
  - 添加 poweredByHeader: false（移除 X-Powered-By 头）
  - 添加 experimental.optimizePackageImports: ['@/components/ui']（优化包导入）

  ✅ 任务 #9: 检查并优化图片 alt 文本
  - 文件：src/components/content/DetailPage.tsx
    - 优化了 hero 图片的 alt 文本：${frontmatter.title} - ${contentType}
  - 文件：src/components/content/NavigationPage.tsx
    - Featured 图片：${title} - Featured ${contentType} guide
    - 列表图片：${title} - ${contentType} guide

  🔧 额外修复

  ✅ 类型定义完善
  - 文件：src/lib/content.ts
  - 在 ContentFrontmatter 接口中添加了 lastModified?: string 字段
  - 确保结构化数据组件的类型安全
