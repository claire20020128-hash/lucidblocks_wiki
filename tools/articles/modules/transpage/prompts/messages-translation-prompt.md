Translate the following JSON messages from English to {language_name}.

IMPORTANT RULES:
1. Translate ALL text values naturally and fluently to {language_name}
2. Keep ALL JSON structure intact (keys, nested objects, arrays)
3. Keep ALL JSON keys unchanged (only translate the values)
4. Maintain the same JSON formatting
5. Preserve all special characters and escape sequences
6. Game name: Use "{game_name}" when referring to the game
7. Keep technical terms and proper nouns appropriately (e.g., "Steam", "Xbox", "PS5")

TARGET LANGUAGE: {language_name} ({lang_code})
GAME NAME IN {language_name}: {game_name}

ORIGINAL MESSAGES IN ENGLISH (JSON):

{content}

CRITICAL: Output ONLY valid JSON directly. DO NOT wrap it in code blocks (```json or ```). DO NOT add any explanations. Start directly with the opening brace {{.

